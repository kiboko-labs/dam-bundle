{% extends 'OroUIBundle:actions:view.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

{% oro_title_set({params : {"%project.name%": entity.names|localized_value} }) %}

{% block pageHeader %}

    {% set additional = [] %}

    {% for node in path %}
        {% set additional = [{
            'indexLabel': node.names|localized_value,
            'indexPath': path('kiboko_dms_node_browse', {uuid: node.id})
        }]|merge(additional) %}
    {% endfor %}

    {% set breadcrumbs = {
        'entity': entity,
        'indexPath': path('kiboko_dms_index'),
        'indexLabel': 'Team Storages'|trans,
        'entityTitle': entity.names|localized_value,
        'additional': additional
    } %}
    {{ parent() }}
{% endblock pageHeader %}

{% block content_data %}
    {% set dataBlocks = [
        {
            'title': 'Directories'|trans,
            'class': 'active',
            'subblocks': [{
                'data': [
                    dataGrid.renderGrid('kiboko-dms-document-nodes-grid', {parent: entity.id})
                ]},
            ]
        }, {
            'title': 'Documents'|trans,
            'class': 'active',
            'subblocks': [{
                'data': [
                    dataGrid.renderGrid('kiboko-dms-documents-grid', {parent: entity.id})
                ]},
            ]
        },
    ] %}
    {% set id = 'projectView' %}
    {% set data = {'dataBlocks': dataBlocks} %}
    {{ parent() }}
{% endblock content_data %}

{% block navButtons %}

    <div class="btn-group">
        {% if is_granted('kiboko_dms_document_upload') %}
            {{ UI.clientButton({
                'dataUrl': path('kiboko_dms_document_upload', {uuid: entity.id}),
                'aCss': 'no-hash',
                'iCss': 'fa-paperclip',
                'label': 'Upload a new file'|trans,
                'widget': {
                    'type': 'dialog',
                    'multiple': false,
                    'reload-grid-name': 'attachment-grid',
                    'options': {
                        'alias': 'attachment-dialog',
                        'dialogOptions' : {
                            'title': 'Upload a new file'|trans,
                            'allowMaximize': false,
                            'allowMinimize': false,
                            'modal': true,
                            'dblclick': 'maximize',
                            'maximizedHeightDecreaseBy': 'minimize-bar',
                            'width': 1000
                        }
                    }
                },
            }) }}
        {% endif %}
    </div>

    {% if is_granted('kiboko_dms_document_upload') and (is_granted('kiboko_dms_node_create') or is_granted('kiboko_dms_node_delete')) %}
        {{ UI.buttonSeparator() }}
    {% endif %}

        {% if is_granted('kiboko_dms_node_create') %}
            {{ UI.deleteButton({
                'aCss': 'btn-danger',
                'path': path('kiboko_dms_node_delete', { uuid: entity.id }),
                'entity_label': 'Folder'|trans,
            }) }}

            {{ UI.clientButton({
                'dataUrl': path('kiboko_dms_node_create', {uuid: entity.id}),
                'aCss': 'no-hash',
                'iCss': 'fa-folder',
                'label': 'Create a new folder'|trans,
                'widget': {
                    'type': 'dialog',
                    'multiple': false,
                    'reload-grid-name': 'attachment-grid',
                    'options': {
                        'alias': 'attachment-dialog',
                        'dialogOptions' : {
                            'title': 'Create a new folder'|trans,
                            'allowMaximize': false,
                            'allowMinimize': false,
                            'modal': true,
                            'dblclick': 'maximize',
                            'maximizedHeightDecreaseBy': 'minimize-bar',
                            'width': 1000
                        }
                    }
                },
            }) }}
        {% endif %}

{% endblock %}

